<div class="jumbotron">
  <h1 class="display-4">Blog in NodeJS</h1>
  <p class="lead">This is a simple hero unit, a simple jumbotron-style component for calling extra attention to featured content or information.</p>
  <hr class="my-4">
  <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
  </p>
</div>

<h2>Publicações recentes</h2>

{{#each posts}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{title}}</h4>
            <p>{{description}}</p>
            <p><a href="/post/{{slug}}">Leia mais...</a></p>
            <small>{{date}}</small> | 
            <small>{{categorie.name}}</small>
        </div>
    </div>
{{else}}
    <p>Ops... Nenhuma publicação pôde ser encontrada.</p>
{{/each}}